apiVersion: v1
kind: Service
metadata:
  name: edge-echo
  namespace: edge-apps
  labels: { app: edge-echo }
spec:
  type: NodePort
  selector: { app: edge-echo }
  ports:
    - { name: http, port: 80, targetPort: 80, nodePort: 30181 }
